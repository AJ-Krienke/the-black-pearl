import React from 'react';
import Recommendation from './PageSections/RecommendationSection/Recommendation';

const MemberPage = props => {
  return (
    <React.Fragment>
      <header></header>
      <main>
        <Recommendation />
      </main>
    </React.Fragment>
  );
};

export default MemberPage;
